{
    "builds": [
        {
            "src": "FE/package.json",
            "use": "@vercel/static-build",
            "config": {
                "distDir": "FE/build"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/FE/build/$1"
        }
    ]
}